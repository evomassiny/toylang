function foo(b, t) {
    if (b) {
        return b;
    }
    return true;
}

let a = 22;
let c = foo(a, x);
let X = 100;

let keep_running = true;
while (keep_running) {
    keep_running = false;
}
X
